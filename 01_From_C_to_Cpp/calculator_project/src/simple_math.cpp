#include "simple_math.hpp"

// We must specify the namespace we are implementing
namespace MathUtils {

    int add(int a, int b) {
        return a + b;
    }

    int subtract(int a, int b) {
        return a - b;
    }

}